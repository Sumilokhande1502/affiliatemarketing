# AffiliateHub (Frontend-Only)

This repository contains a frontend-only React application located in `frontend/`.

## Run locally

```bash
cd frontend
yarn install
yarn start
```

## Build

```bash
cd frontend
yarn build
```

## Deployment

Use Vercel with the root-level `vercel.json` included in this repo.
